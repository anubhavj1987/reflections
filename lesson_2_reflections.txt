what happens when you initialize a repository? Why do you need to do it?

When we initialize repo, it adds hidden file ".git" to current directory that keeps track of commit history.

How is the staging area different from the working directory and the repository? What value do you think it offers?

staging area is intermediate step that keeps record of added files to commit later.
It offers to commit specific set of files rather they commiting everything.

How can you use the staging area to make sure you have one commit per logical change?

stating area allows us to add only modified files to it which can be commit later on.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

branches can help in creating seperate mode like adding experimental features etc. we can keep original implementation in master branch and experimental in other branches.

How do the diagrams help you visualize the branch structure?

using diagram we can easily track reachable and unreachable commits.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

merging two branches together will get changes in both branches into one.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?



